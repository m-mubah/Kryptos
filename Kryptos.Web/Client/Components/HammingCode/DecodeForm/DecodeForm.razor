@using Kryptos.Web.Client.Models.HammingCode
<EditForm Model="@FormData" OnValidSubmit="@ValidFormSubmit" autocomplete="off">
    <DataAnnotationsValidator/>
    <div class="mb-6">
        <InputText @bind-Value="FormData.Value"
                   class="block p-4 w-full text-gray-900 bg-gray-50 rounded-lg border border-gray-300 sm:text-md focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500">
        </InputText>
        <p class="validation-error">
            <ValidationMessage For="() => FormData.Value"/>
        </p>
    </div>

    <div class="mb-6">
        <InputRadioGroup @bind-Value="FormData.Parity">
            <div class="flex items-center mb-4">
                <InputRadio Value="Parity.Odd"
                            class="w-4 h-4 border-gray-300 focus:ring-2 focus:ring-violet-300 dark:focus:ring-violet-600 dark:focus:bg-violet-600 dark:bg-gray-700 dark:border-gray-600"/>
                <label class="block ml-2 text-sm font-medium text-gray-900 dark:text-gray-300">
                    Odd
                </label>
            </div>

            <div class="flex items-center mb-4">
                <InputRadio Value="Parity.Even"
                            class="w-4 h-4 border-gray-300 focus:ring-2 focus:ring-violet-300 dark:focus:ring-violet-600 dark:focus:bg-violet-600 dark:bg-gray-700 dark:border-gray-600"/>
                <label class="block ml-2 text-sm font-medium text-gray-900 dark:text-gray-300">
                    Even
                </label>
            </div>
        </InputRadioGroup>
        <p class="validation-error">
            <ValidationMessage For="() => FormData.Parity"/>
        </p>
    </div>

    <button type="submit" disabled="@ButtonDisabled"
            class="@(EditContext.Validate() ? "btn-primary" : "btn-primary-disabled")">
        Decode
    </button>
</EditForm>

@if (ShowResult)
{
    <DecodeResult Result="Result"></DecodeResult>
}